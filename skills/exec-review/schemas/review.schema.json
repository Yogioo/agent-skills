{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["clean", "refined"]
    },
    "note": { "type": "string" }
  },
  "required": ["status", "note"],
  "additionalProperties": false
}
