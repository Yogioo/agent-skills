/** @deprecated Import from normalize-event.mjs; kept for backward compatibility. */
export {
  normalizeAgentEvent,
  normalizeEvent,
  normalizeCodexEvent,
  normalizePiEvent,
  detectSource,
  extractOutTextFromRaw,
  extractJsonFromText,
  extractJsonFromEventsFile,
} from './normalize-event.mjs'
